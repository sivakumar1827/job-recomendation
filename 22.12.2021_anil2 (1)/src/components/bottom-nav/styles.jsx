import { makeStyles } from '@material-ui/styles';

export default makeStyles((theme) => ({
  root: {
    overflow: 'auto',
    position: 'fixed',
    bottom: 0,
    width: '100%',
    height:'10vh',
    backgroundColor: '#DADADA',
  },
}));
0 